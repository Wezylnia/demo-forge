# DemoForge.Media

`DemoForge.Media` is reserved for optional media exporters such as GIF and MP4.

The MVP build keeps the project present in the solution so the architecture has a stable extension point, but media rendering is intentionally not implemented yet.
